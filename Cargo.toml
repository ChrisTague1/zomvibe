[package]
name = "zomvibe"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.15", features = ["dynamic_linking"] }
rand = "0.8"
serde = { version = "1", features = ["derive"] }
ron = "0.8"
